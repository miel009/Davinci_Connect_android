<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DaVinciConnect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@drawable/fondo_claro</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="alertDialogTheme">@style/CustomAlertDialog</item>
    </style>

    <style name="Theme.DaVinciConnect" parent="Base.Theme.DaVinciConnect" />

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="colorAccent">#D1C4E9</item> <!-- Light Violet for buttons -->
        <item name="android:textColorPrimary">@android:color/white</item> <!-- Title color -->
        <item name="android:textColor">#E0E0E0</item> <!-- Body text color -->
    </style>

    <style name="CustomPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:background">#333333</item> <!-- Dark background -->
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
